TELEGRAM_BOT_TOKEN=
LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL=gpt-4o-mini
DATABASE_PATH=./data/workshop.db

# WB: enter locally only, never through Telegram. Restart after changing.
# .env is plaintext: restrict filesystem permissions and backups.
WB_API_TOKEN=
# Non-secret rate policy: personal/service only if confirmed by the owner.
# Empty/unknown uses conservative application intervals.
WB_API_PROFILE=
